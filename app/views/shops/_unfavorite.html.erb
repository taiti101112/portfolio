<%= button_to favorite_path(current_user.favorites.find_by(shop_id: shop.id), format: :html), id: "unfavorite-button-for-shop-#{shop.id}", method: :delete, class: "btn btn-primary" do %>
  <i class="bi bi-heart-fill"></i>
<% end %>
