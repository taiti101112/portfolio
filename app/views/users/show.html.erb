<div class="user-profile-container container py-5">
  <div class="card shadow-lg border-0">
    <div class="card-header bg-gradient text-white text-center py-4" style="background: linear-gradient(135deg, #6f42c1, #007bff);">
      <h2 class="mb-0"><i class="bi bi-person-circle me-2"></i><%= @user.name %>のマイページ</h2>
    </div>

    <div class="card-body text-center">
      <div class="user-avatar mb-4">
        <i class="bi bi-person-circle display-1 text-muted"></i>
      </div>

      <div class="user-actions d-flex justify-content-center gap-4 mb-4">
        <%= link_to "プロフィール編集", edit_user_registration_path, class: "btn btn-outline-primary btn-lg rounded-pill shadow-sm px-4" %>
        <%= link_to "お気に入り一覧", favorites_shops_path, class: "btn btn-outline-success btn-lg rounded-pill shadow-sm px-4" %>
      </div>

      <p class="text-muted lead">ようこそ、<%= @user.name %>さん！プロフィールの編集や、お気に入りショップの管理ができます。</p>
    </div>

    <div class="card-footer text-center bg-light py-3">
      <small class="text-muted">登録日: <%= @user.created_at.strftime("%Y年%m月%d日") %></small>
    </div>
  </div>
</div>
